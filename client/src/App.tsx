/* Editorial Property Journal direction: the single public landing page keeps navigation, conversion and trust content in one scrollable experience. */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
